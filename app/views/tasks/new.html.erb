<% content_for :title, t('tasks.new_task') %>

<div class="container max-w-6xl mx-auto px-4 py-8">
  <!-- Page Header -->
  <div class="mb-8">
    <h1 class="text-4xl font-bold text-base-content flex items-center gap-3">
      <%= t('tasks.new_task') %>
    </h1>
    <p class="text-base-content/70 mt-2"><%= t('tasks.new_task_description') %></p>
  </div>

  <!-- Form -->
  <%= render "form", task: @task %>
</div>
